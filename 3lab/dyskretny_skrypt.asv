clear all;
close all;

k1 = 5;
k2 = 0.1;

Td = [0.01, 0.05, 0.1, 0.3, 0.5, 0.8, 1, 2];
c = ['k', 'b', 'r', 'g', 'c', 'm', 'o', 'y'];

figure(1);
hold on;
grid on;
% for i = 1 : length(Td)
%     Tda = Td(i);
%     [t] = sim('second_2');
%     plot(Tda, t.w);
    

% for i = [0.01, 0.5]
%        dt = i;
%        sim('second_2');
%        plot(dt, ans.w(end), 'o', 'linewidth', 3);
%    end
    %plot(t.tout, t.w, c(i));
end
